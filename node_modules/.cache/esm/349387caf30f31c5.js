let bcrypt,jwt;_f30‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_f30‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);const User = require("../models/user.model.js");



exports.register = (req, res) => {
  console.log("register log");

  // Register a User
  bcrypt.hash(req.body.password, 10).then(function (hash) {
    const user = new User({
      name: req.body.name,
      password: hash,
    });
    // Save Note in the database
    user
      .save()
      .then((data) => {
        _f30‍.g.console.log("register then", data);
        res.send(data);
      })
      .catch((err) => {
        res.status(500).send({
          message: err.message || "Some error occurred while creating the User.",
        });
      });
  });
};

exports.login = async (req, res) => {
  console.log("login log");
  const user = await User.findOne({ name: req.body.name });
  if (!user) {
    return res.status(400).send({
      message: "No User Found",
    });
  }
  const result = await bcrypt.compare(req.body.password, user.password);
  if (!result) {
    return res.status(400).send({
      message: "Password Mismatched",
    });
  }
  const token = await jwt.sign({ name: req.body.name, password: req.body.password }, "secret", {
    expiresIn: "24h",
  });
  res.send({
    token: token,
    userId: user._id,
  });
};
